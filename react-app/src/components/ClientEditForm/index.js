import React from 'react';


export default function ClientEditForm({clients}){
    const handleSubmit = (e)=>{
        e.preventDefault();
        
    }
    return(
        <form onSubmit={handleSubmit}>

        </form>
    )
}